name: previsao_do_tempo
description: Um aplicativo Flutter de previsao do tempo.

publish_to: "none" # Define como "none" para evitar a publicação

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5 # Dependencia HTTP para requisicoes de API

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
